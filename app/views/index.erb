<div id="peeps"
	<%if @peeps%>
		<% @peeps.each do |peep| %>
		<% @user = User.first(:id => peep.user_id)%>
		<ul>
		<li><%=@user.name%>@<%=@user.user_name%><%=peep.time_added.strftime("%Y-%m-%d %H:%M:%S")%><br/><%=peep.message%></li>
		</ul>
	<%end%>
	<%end%>
</div>

<div id="post-a-peep">
<a href="#">Post a Peep</a>
</div>




